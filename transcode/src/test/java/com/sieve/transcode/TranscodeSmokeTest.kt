package com.sieve.transcode

import org.junit.Assert.assertEquals
import org.junit.Test

/** Task 0: proves the :transcode pure-JVM unit-test source set is wired. */
class TranscodeSmokeTest {
    @Test fun sanity() = assertEquals(4, 2 + 2)
}
